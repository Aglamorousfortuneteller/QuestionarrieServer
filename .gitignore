from pathlib import Path

project_structure = {
    "QuestionarrieDemo": [
        "index.html",
        "instructions.html",
        "questionnaire.html",
        "admin.html",
        "style.css",
        "script.js",
        "questions.json",
        "loginData.json",
        "responses.json",
        "responses.csv",
        "video.mp4",
        "README.md",
        "app.py",
        ".gitignore"
    ]
}

# Create the structure locally
root = Path("/mnt/data/QuestionarrieDemo")
root.mkdir(exist_ok=True)

for file in project_structure["QuestionarrieDemo"]:
    (root / file).touch()

# Example .gitignore content
gitignore_content = """
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.pyo

# Virtual environment
.venv/
env/
venv/

# Flask cache
instance/
.webassets-cache

# PyInstaller
*.spec

# Response data
responses.json
responses.csv

# OS files
.DS_Store
Thumbs.db
"""

with open(root / ".gitignore", "w", encoding="utf-8") as f:
    f.write(gitignore_content)

"Готово. Структура создана и доступна как архив."
